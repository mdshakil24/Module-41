
// export default add;

// function add(first,second) {
//     return first + second;
// }

const add = (first,second) => {
    return first + second;
}

const subtraction = (first,second) => {
    return second - first;
}

const dividedTwoNumberOnSuglassComponents = (first,second) => {
    return second / first;
}

// const calculator = {add:add, subtraction:sub}
// export default calculator;

export {add, subtraction, dividedTwoNumberOnSuglassComponents};
